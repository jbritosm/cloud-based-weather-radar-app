# Alembic (database migrations). The database URL is not here: migrations/env.py reads it from the
# application settings (DATABASE_URL), so the same code path is used by the app and by the CLI.
[alembic]
script_location = migrations
